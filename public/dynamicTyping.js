var options = {
    strings: ['satisfy', 'build', 'innovate', 'scale', 'create'],
    typeSpeed: 100,
    loop: true,
    backSpeed: 80,
    startDelay: 1000,
    backDelay: 1500
};

var typed = new Typed('.type', options);